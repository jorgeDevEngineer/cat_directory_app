import 'package:flutter/material.dart';

class AppColors {
  // Option A — Warm Amber Palette
  static const Color primary = Color(0xFFE65100);       // Warm Deep Orange
  static const Color primaryLight = Color(0xFFFF9800);  // Bright Amber
  static const Color primaryDark = Color(0xFFBF360C);   // Terracotta / Burnt Orange
  static const Color accent = Color(0xFFFFB300);        // Gold / Amber Accent

  static const Color background = Color(0xFFFFF8E1);    // Soft Cream
  static const Color surface = Color(0xFFFFFFFF);       // Pure White for Cards
  static const Color surfaceVariant = Color(0xFFFFF3E0);// Light Warm Orange Tint

  static const Color textPrimary = Color(0xFF212121);   // Dark Charcoal
  static const Color textSecondary = Color(0xFF757575); // Medium Gray
  static const Color textLight = Color(0xFF9E9E9E);     // Muted Gray

  static const Color success = Color(0xFF2E7D32);       // Forest Green
  static const Color warning = Color(0xFFF57C00);       // Orange Warning
  static const Color error = Color(0xFFD32F2F);         // Muted Red

  static const Color border = Color(0xFFFFE0B2);        // Soft Amber Border
}
